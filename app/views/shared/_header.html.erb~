<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to 'Camping<img src="/assets/logo.jpg"/>Recipes<span class="brand-tld">.co</span>'.html_safe, root_path, class: "brand" %>
      <ul class="nav pull-right">
      <% if user_signed_in? %>
		    <li><%= link_to '<i class="icon-user"></i> Profile'.html_safe, user_path(current_user) %></li>
		    <li><%= link_to '<i class="icon-off"></i> Sign Out'.html_safe, destroy_user_session_path, method: "delete" %></li>
	    <% else %>
	      <li><%= link_to '<i class="icon-plus-sign"></i> Sign Up'.html_safe, new_user_registration_path %></li>
        <li><%= link_to '<i class="icon-off"></i> Sign In'.html_safe, new_user_session_path %></li>
	    <% end %>
	    </ul>
    </div>
  </div>
</div>

<% provide(:title, "Edit '#{@recipe.title}' Recipe") %>
<% provide(:desc, "") %>

<div class="span8">
  <h1>Edit <%= @recipe.title %> Recipe</h1>

  <%= render 'form' %>
</div>

<div class="span4">
  <%= render 'shared/sidebar' %>
</div>
